export const publicRoutes = [
  "/",
  "/pedido-domicilio",
  "/restaurantes",
  "/reservas"
]

export const authRoutes = [
  "/auth/login"
]

export const apiAuthPrefix = "/api/auth"

export const DEFAULT_LOGIN_REDIRECT = "/restaurante/create"